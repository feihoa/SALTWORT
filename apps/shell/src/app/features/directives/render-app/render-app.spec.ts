import { RenderComponent } from '.';

describe('RenderComponent', () => {
  it('should create an instance', () => {
    const directive = new RenderComponent();
    expect(directive).toBeTruthy();
  });
});
