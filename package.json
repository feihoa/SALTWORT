{
  "name": "salt-wort",
  "private": false,
  "scripts": {
    "shell": "npm run start --prefix apps/shell -- --port 4200",
    "bridge": "npm run start --prefix apps/bridge-pattern -- --port 4201",
    "all": "concurrently \"npm run shell\" \"npm run bridge\""
  }
}